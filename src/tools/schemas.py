"""JSON tool schemas for the Anthropic Messages API.

One schema per QC tool (with parameter ranges) that the agent can call. See the
tool inventory in CLAUDE.md §7.

Implemented in Phase 2 (see CLAUDE.md §12).
"""
