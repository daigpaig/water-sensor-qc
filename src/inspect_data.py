"""Load and summarise a water-quality time series.

Reads a CSV with a datetime column + one or more numeric columns and produces a
summary (rows, time range, inferred frequency, NaN count/%, per-column
min/max/mean/std).

Implemented in Phase 1 (see CLAUDE.md §12).
"""
